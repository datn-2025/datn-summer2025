<?php

return [
    'options' => [
        "closeButton"       => true,              // Hiện nút đóng
        "debug"             => false,             // Tắt debug
        "newestOnTop"       => true,              // Thông báo mới nhất hiển thị trên cùng
        "progressBar"       => true,              // Hiển thị thanh tiến trình
        "positionClass"     => "toast-top-right", // Vị trí góc trên bên phải
        "preventDuplicates" => true,              // Ngăn thông báo trùng lặp
        "onclick"           => null,

        "showDuration"      => "300",              // Thời gian hiệu ứng hiển thị (ms)
        "hideDuration"      => "1000",             // Thời gian hiệu ứng ẩn (ms)
        "timeOut"           => "4000",             // Thời gian hiện thông báo (ms)
        "extendedTimeOut"   => "1500",             // Thời gian kéo dài khi hover (ms)

        "showEasing"        => "swing",            // Hiệu ứng easing hiển thị
        "hideEasing"        => "linear",           // Hiệu ứng easing ẩn
        "showMethod"        => "fadeIn",           // Hiệu ứng hiển thị
        "hideMethod"        => "fadeOut",          // Hiệu ứng ẩn
    ],
];
